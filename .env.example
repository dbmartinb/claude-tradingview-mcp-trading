# BitGet API credentials
# Get these from: BitGet account → API Management
# Withdrawals: OFF. IP whitelist: ON.
BITGET_API_KEY=your_api_key_here
BITGET_SECRET_KEY=your_secret_key_here
BITGET_PASSPHRASE=your_passphrase_here
BITGET_BASE_URL=https://api.bitget.com

# Trading mode: "spot" or "futures"
TRADE_MODE=spot

# Portfolio value — used to calculate position size (1% risk per trade)
PORTFOLIO_VALUE_USD=1000

# Trade limits — these are your guardrails
MAX_TRADE_SIZE_USD=100        # No single trade can exceed this value
MAX_TRADES_PER_DAY=3          # Per strategy — bot stops after this many trades per strategy per day

# Symbol and timeframe
SYMBOL=BTCUSDT
TIMEFRAME=4H

# Set to "true" to run in paper trading mode (logs decisions but never places real orders)
PAPER_TRADING=true

# Strategy to execute: vwap_scalp | ema_cross | bb_rsi | macd
# All 4 strategies are evaluated and logged to trades.csv on every run.
# This setting controls which one actually places orders.
# Leave on vwap_scalp to start — compare strategies in trades.csv after 2 weeks.
STRATEGY=vwap_scalp
